package suanfa;

/**
 * 快速排序
 * <p>
 * 1.从数组中找一个基准数，然后把该数组分为三份，
 * 2.比基准数小或者等于的放在左边，基准数，比基准数大或者等于的放在右边。
 * 然后再对左边和右边进行上述1，2步骤操作
 */
public class QuickSort {

    /**
     * 快速排序
     *
     * @param data  数据
     * @param left  左边开始角标
     * @param right 右边开始角标
     */
    public static void qSort(int[] data, int left, int right) {
        //基准数，取序列的最左边
        int base = data[left];

        int ll = left;
        int rr = right;




    }
}
